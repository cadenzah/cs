#include <iostream>
#include <set>
#include <algorithm>
using namespace std;
// set을 사용해서 풀어보자
// max_element, min_element 사용해보자

int main(void) {
  ios::sync_with_stdio(false);
  cin.tie(NULL);

  int count, temp;
  set<int> numbers;
  set<int>::iterator max_it, min_it;

  cin >> count;

  return 0;
}
